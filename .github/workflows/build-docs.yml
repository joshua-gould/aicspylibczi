name: Docs

on:
  push:
    tags:
      - 'docs'

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        with:
          submodules: true
      - name: Setup Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install Depenencies
        run: |
          pip install --upgrade pip
          pip install .[dev]
      - name: Generate Docs
        with:
          BUILD_DOCS: true
        run: |
          mkdir cmake_build_debug
          cd cmake_build_debug
          cmake ..
          cmake --build . --target docs
      - name: Publish Docs
        uses: JamesIves/github-pages-deploy-action-python@releases/v3
        with:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          BASE_BRANCH: release/aicspylibczi
          BRANCH: gh-pages
          FOLDER: cmake-build-debug/docs/html
